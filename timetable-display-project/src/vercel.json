{
  "version": 2,
  "builds": [
    { "src": "src/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/src/$1" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/src/DisplayTimetable.html" }
  ]
}